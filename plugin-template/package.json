{
  "name": "my-dian-plugin",
  "version": "1.0.0",
  "description": "我的 Dian 插件",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "pack": "npm run build && node scripts/pack.mjs"
  },
  "devDependencies": {
    "@dian/plugin-runtime": "file:../packages/plugin-runtime",
    "reflect-metadata": "^0.2.2",
    "tsup": "^8.5.0",
    "typescript": "^5.6.3"
  }
}
